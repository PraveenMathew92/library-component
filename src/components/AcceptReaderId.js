import React from 'react'; 

const AcceptReaderId = () => (
    <div>
        <input placeholder="Reader Id" />
        <br />
        <button type="submit">SUBMIT</button>
    </div>
);

export default AcceptReaderId;